#!/usr/bin/env bash

function disk_cheatsheet
{
  echo -e "使用ls命令查看设备文件时，不会显示size,而会显示主设备号和次设备号"
  echo -e "主设备号标识设备所属的驱动程序，不同类型的设备有不同的主设备号"
  echo -e "次设备号用于标识同一类型设备中的具体实例"
  echo -e ""

}

function partition_cheatsheet
{
  echo -e "分区: 将存储设备划分成不同的区域，以便组织和管理存储空间.每个分区通常都会格式化为特定的文件系统"
  echo -e "分区的信息保存在分区表中。分区表格式有mbr(ms-dos)、gpt,它们是跨平台的"
  echo -e ""
  echo -e "mbr分区表中，存在主分区、扩展分区和逻辑分区"
  echo -e "\t 主分区：每个硬盘最多可以有4个主分区。主分区可以包含一个操作系统"
  echo -e "\t 扩展分区：不包含文件系统，是用来包含逻辑分区的容器"
  echo -e "\t 逻辑分区：为了创建更多的分区，可以将一个主分区划分为一个扩展分区，再将扩展分区划分为逻辑分区。逻辑分区可以包含文件系统，能被操作系统识别和访问"
  echo -e ""
  echo -e "GPT分区表中直接使用分区类型来标识分区的类型,如EFI分区、MSR分区（微软保留分区）"

  echo -e "查看分区"
  echo -e "\t fdisk -l disk_name"
  echo -e "\t lsblk disk_name"

  echo -e ""

  echo -e "修改分区(需要root权限)"
  echo -e "\t fdisk disk_name （先创建分区表，再新建分区，最后保存）"
  echo -e "\t parted"

  echo -e "格式化分区/在分区上建立文件系统"
  echo -e "\t mkfs -t fs_type partition_name"

  echo -e "将格式化后的分区挂载到目录上"
  echo -e "\t mount -t fs_type partition_name path"
}

function file_cheatsheet
{
  echo -e "目录文件的权限："
  echo -e "\t 读权限（r）：允许列出目录中的文件和子目录"
  echo -e "\t 写权限（w）：允许在目录中创建、删除和重命名文件和子目录"
  echo -e "\t 执行权限（x）：允许进入目录，也就是访问目录中的文件和子目录"
  echo -e "目录权限通常设置为755"
  echo -e ""

  echo -e "/tmp目录的权限是rwxrwxrwt，t为Sticky Bit, "
  echo -e "用于目录，表示只有目录的所有者、文件的所有者和root用户才能删除或移动目录中的文件"
  echo -e ""

  echo -e "使用lsattr/chattr来查看/修改文件的其它特殊权限"

}

# chown -R # top_cheatsheete f
# # user_cheatsheet
# chgrp
# whatis
# info
# ls -l /home/firma -l是选项，/home/firma是参数
# grep -i "pattern" file.txt -i "pattern"是带参数的选项
# 最初usr目录确实是用户家目录，但现在是Unix System Resources目录
# head -n -10 不显示前10行
# tail -n -10 不显示后10行
#! /bin/bash

echo -e '\033[38;2;102;153;204;1m'

cat << EOF

bash的键绑定默认为emacs风格,如下所示:
(您可以使用 set -o vi切换键绑定为vi风格)

ctrl-p 相当于向上箭头
ctrl-n 相当于向下箭头

ctrl-a 将光标移动到行首
ctrl-e 将光标移动到行尾

ctrl-b 将光标向前移动一个字符(back)
ctrl-f 将光标向后移动一个字符(forward)
alt-b 将光标向前移动一个单词(back)
alt-f 将光标向后移动一个单词(forward)

ctrl-u 清空当前行
ctrl-d 删除光标后的一个字符
ctrl-h 删除光标前的一个字符

ctrl-k 剪切光标后的所有字符

ctrl-w 删除前一个单词
alt-d 删除下一个单词

ctrl-y 撤销一步操作

alt-l 将光标后的所有字符转换为小写
alt-u 将光标后的所有字符转换为大写

alt-. 上一条命令的最后一个参数,相当于!$

man和less等命令，在浏览文件内容时，有以下快捷键:
/pattern - 搜索pattern
n - 查找下一个匹配项
N - 查找上一个匹配项
g - 移动到文件头
G - 移动到文件尾
b / Page UP 向上翻滚一页
space / Page Down 向下翻滚一页
回车 向下滚动一行

apropos key  根据给定的关键字在man手册的whatis entries中搜索, 并列出匹配的结果
info cmd 查看命令的info文档

cd - 进入上次操作的目录
cd ~ 或 直接 cd 回到用户家目录
cd ./path/to 相当于 cd path/to

which 打印命令的绝对路径

tail -f filename 实时查看文件内容

对于目录来说，读、写、执行权限意味着该目录可以被浏览(ls)；新建、移动、删除文件；进入(cd)。

------特殊文件权限------
------------------------------------
passwd命令的可执行文件的权限为 rws r-x r-x
你可以通过以下命令查看 passwd 的确切权限：ls -l $(which passwd)
关键点是 "s" 位（SUID 位）。这意味着：
文件的所有者是 root
任何用户执行此命令时，它会以 root 权限运行
普通用户可以执行此命令来更改自己的密码
------------------------------------
/tmp 目录的权限 为 rwx rwx rwt
最后的 't' 表示"粘滞位"（Sticky Bit）, 影响文件的删除权限
root外的用户只能删除自己拥有的文件，即使该目录对所有人都有写权限


ln file link 创建硬链接
ln -s item link 创建符号链接, 可以是目录
建立符号链接的目的是为了克服硬链接的两个缺点:硬链接只能在同一个文件系统内创建, 硬链接不能关联目录,只能是文
件。
硬链接是在同一个文件系统中创建多个目录项指向同一个inode,共享同一份数据。
软链接是一个特殊的文件,其inode中存储的是指向目标文件或目录的路径信息。
文件系统基础：

Linux 文件系统为每个文件分配一个唯一的索引节点（inode）号。
inode 存储文件的元数据，如权限、所有者、大小、时间戳等，但不包含文件名。
文件名和 inode 号的映射关系存储在目录项中。
硬链接：

硬链接是文件系统中的目录项，指向同一个 inode。
创建硬链接不会生成新的 inode，只是增加了一个指向现有 inode 的目录项。
硬链接的限制：
不能跨文件系统（因为 inode 号在不同文件系统中可能重复）。
不能用于目录（防止文件系统形成环）。
硬链接的特性：
所有硬链接都是平等的，没有原始文件和链接文件的区别。
修改任一硬链接，都会影响到同一 inode 的所有链接。
inode 的链接计数记录了指向它的硬链接数量。
只有当最后一个硬链接被删除时，文件的数据才会被释放。
软链接（符号链接）：

软链接是一种特殊的文件，它包含了目标文件或目录的路径信息。
软链接有自己的 inode 和数据块，存储的是目标的路径。
软链接的特性：
可以跨文件系统。
可以链接到目录。
可以指向不存在的文件（悬空链接）。
删除原文件不会自动删除软链接，但会导致软链接失效。
可以使用 readlink 命令查看软链接指向的路径。
硬链接与软链接的比较：

硬链接直接指向 inode，软链接指向文件路径。
硬链接的 inode 号与原文件相同，软链接有自己的 inode 号。
硬链接的文件大小与原文件相同，软链接的文件大小是路径字符串的长度。
删除原文件对硬链接没有影响，但可能导致软链接失效。
链接的应用：

硬链接可用于创建文件的备份或防止误删。
软链接常用于创建快捷方式或管理不同版本的文件/目录。
在库管理中，软链接被广泛用于维护不同版本的动态库，允许更新库文件而不需要重新编译依赖它的程序。
文件操作对链接的影响：

移动硬链接的目标文件不会影响硬链接。
移动软链接的目标文件可能会导致软链接失效，除非更新软链接的路径。
创建链接：

创建硬链接：ln 目标文件 链接名
创建软链接：ln -s 目标文件或目录 链接名
安全性考虑：

硬链接共享相同的访问权限，而软链接有自己的权限设置。
在处理符号链接时，需要注意符号链接攻击的可能性。

linux对存放在磁盘中的每一个文件，都分配一个索引节点号。文件名称和索引节点号组成的对（即path string -> inode）称为链接。

a和b其实是同一个文件，修改a文件的内容，我们会发现b文件的内容也发生了相应的变化。

与某个文件关联的所有硬链接文件（类似于指针）都被删除后，这个文件才会被真正删除。因此建立硬链接文件可以防止误删。

每个分区都有自己的索引节点号数组，因此不同的分区可能有相同的索引节点号，因此不能在不同的分区建立硬链接。硬链接也不能链接目录。为解决这一问题，引入了符号链接（软链接）。

硬链接和软链接类似于指针和二级指针。

硬链接并不创建文件，只是新增了一条path string -> inode的映射，inode的引用计数加1。将被链接文件移动到其它目录，硬链接仍然有效。

软链接是一个真正的文件，它保存了原文件的路径（readlink命令可以输出符号文件的内容，我们可以通过该命令验证），进而能访问到原文件。删除被链接文件，新建同名文件，软链接会自动链接到新的文件。

c文件中保存了a文件的相关信息，进而可以通过c文件访问a；而b文件可以直接访问a。（软链接会真正建立自己的 inode 索引和 block，所以软链接和源文件的 inode 号是不一致的，而且在软链接的 block 中，写的不是真正的数据，而是源文件的路径。）

我们可以为动态库创建软链接，动态库的版本可以变化，只要软链接不变，调用库的方式也无须改变。

stat filename 显示文件或文件系统的详细信息

EOF
